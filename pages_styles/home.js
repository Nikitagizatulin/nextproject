import styled from 'styled-components';

const Main = styled.div`
  font-family: 'Quando', serif;
  .home-page_title,
  .home-page_subtitle {
    text-align: center;
    color: #1890ff;
    font-weight: 500;
    max-width: 80vw;
    margin-left: auto;
    margin-right: auto;
  }
`;
export default Main;
