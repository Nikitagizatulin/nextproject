import styled from 'styled-components';

const Home = styled.div`
    font-family: 'Quando', serif;
    .statistic {
        font-size: 25px;
    }
    .title-statistic{
      margin-top: 20px;
    } 
    .statisticNumber {
        margin-left: 8px;
        font-size: 34px;
        color: #1890ff;
    }
`;
export default Home;
